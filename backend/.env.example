# PostgreSQL (Aurora) connection
DB_HOST=your-aurora-cluster-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# Set to "true" if Aurora requires SSL (recommended for production)
DB_SSL=true

# Server
PORT=5000
